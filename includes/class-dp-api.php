<?php
if(! defined('ABSPATH') ){ exit(); }

/**
*
*
*/

class DP_API{

	// provide endpoints listen and delegate requests
	// set strict access paths to data an no more
	// authenticate nonces (maybe send nonces)
	// convert data before sending it 
}